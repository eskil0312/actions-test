
/**
 * Note: will always return string
 * 
 * @example
 * const toke = useAppAuth("testApp");
 * @returns
 * authtoken string
 * @param
 * appId -string
 */
const useAppAuth = (appId: string) => {
    return "auttoken"
}
export default useAppAuth